# DoThis Athlete Signal Split handoff

## Athlete-facing outcome

The homepage now shares the training library’s visual identity: near-black green-cast ground, chalk-white Archivo commands, signal green, cool blue arena light, fine grain, quiet rules, and compact actions. The page remains mysterious and motivating while stating one truthful mechanism: the athlete sets the sport and week before DoThis builds around it.

The first viewport contains the approved “You can be lethal” promise, synchronized athlete media, all six sport choices, one primary action, and exact Premium Preview terms. The rest of the page uses three restrained beats: week-led method, one real Progress screen, and “Make them notice.”

## Production implementation

- `index.html`: Athlete Signal Split narrative, approved direction contract, semantic selector, product proof, legacy fragments, legal/footer navigation, SEO metadata, and structured data.
- `home.css`: article-family palette and typography, four-chapter pacing, exact mobile/tablet reflow, responsive athlete planes, signal-sweep motion, focus/touch states, and reduced-motion handling.
- `home.js`: fixed sport allowlist, synchronized portrait/line/accent/URL/CTA state, browser history, keyboard navigation, preloading, and existing local CTA analytics semantics.
- `assets/awakening/mobile/`: six `780 × 658` responsive campaign derivatives preserving face, full stance, and equipment.
- `scripts/build-mobile-athlete-assets.mjs`: deterministic derivative builder.
- `scripts/redesign-production-check.mjs`: 503-assertion production interaction, accessibility, responsive, route, fragment, and fallback suite.

## Preserved boundaries

- The native app and backend were not changed.
- Every `/app` redirect and fixed App Store custom-product-page identifier remains unchanged.
- Real app UI was not fabricated, repainted, rotated, or stacked.
- Approved logo/icon were not modified.
- Articles, comparisons, legal/support pages, sitemap behavior, canonical URLs, and public build allowlist remain intact.
- No testimonials, metrics, pricing, medical outcomes, guaranteed athletic outcomes, or runtime image-generation feature were added.

## Verification

- Production suite: `PASS athlete signal split: 503 assertions`.
- Site integrity: 63 HTML pages, 47 articles, 56 sitemap URLs.
- Lighthouse mobile: `98 / 100 / 100 / 100`; LCP `2.3s`; TBT `0ms`; CLS `0`.
- Impeccable final disposition: `ship` after both requested fixes were scored resolved.
- UI/UX final disposition: `ship`.
- Beauty score: `91/100`, passing the user-requested `≥90` gate.
- Public secret scan: passed.

## Local preview

```bash
npm run build:public
python3 -m http.server 4174 --bind 127.0.0.1 --directory _site
```

Open `http://127.0.0.1:4174/`. Append `?sport=soccer`, `basketball`, `pickleball`, `tennis`, or `volleyball` to inspect synchronized states.

## Deliberately unresolved

- Verify each `ppid` gallery on a physical iPhone/App Store account. Browser automation validates destination construction and identifiers only.
- No owner-approved testimonials, Premium pricing, or measured outcome claims were supplied; none were added.

## Release status

Complete and verified locally. Not deployed, published, pushed, or committed by this task.
